---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: pod-1
  labels:
    app: swiggy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: swiggy
  template:
    metadata:
      labels:
        app: swiggy
    spec:
      containers:
        - name: cont-1
          image: ubuntu
          ports:
            - containerPort: 80
          command: ["/bin/bash", "-c", "while true; do echo Welcome to FLM; sleep 5; done"]
          volumeMounts:
            - name: rbu-volume
              mountPath: "/tmp/devops"

        - name: cont-2
          image: centos
          ports:
            - containerPort: 80
          command: ["/bin/bash", "-c", "while true; do echo Welcome to FLM; sleep 5; done"]
          volumeMounts:
            - name: rbu-volume
              mountPath: "/tmp/aws"

      volumes:
        - name: rbu-volume
          emptyDir: {}
